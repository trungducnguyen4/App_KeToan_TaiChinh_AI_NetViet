{
  "name": "@workit/web",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "set NEXT_DIST_DIR=.next-dev&& set NODE_OPTIONS=--openssl-legacy-provider&& next dev -p 3000",
    "dev:fresh": "node scripts/clean-next.cjs && set NEXT_DIST_DIR=.next-dev&& set NODE_OPTIONS=--openssl-legacy-provider&& next dev -p 3000",
    "clean": "node scripts/clean-next.cjs",
    "build": "set NEXT_DIST_DIR=.next-build&& set NODE_OPTIONS=--openssl-legacy-provider&& next build",
    "start": "set NEXT_DIST_DIR=.next-build&& set NODE_OPTIONS=--openssl-legacy-provider&& next start -p 3000",
    "lint": "next lint"
  }
}
